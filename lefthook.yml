output:
  - failure
  - execution_out

pre-commit:
  commands:
    format-and-lint:
      glob: "web/src/**/*.{ts,tsx,css}"
      root: "web/"
      run: pnpm exec prettier --write {staged_files} && pnpm exec eslint --fix {staged_files} || true; git add {staged_files}
